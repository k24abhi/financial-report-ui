
  # Simplify Document Upload UI

  This is a code bundle for Simplify Document Upload UI. The original project is available at https://www.figma.com/design/Oj0Ymm8WDwyxt2SBW0PITx/Simplify-Document-Upload-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  